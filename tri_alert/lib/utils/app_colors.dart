import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Splash / Auth (azul oscuro) ─────────────────────────────────────
  static const Color backgroundDark  = Color(0xFF050D2A);
  static const Color backgroundMid   = Color(0xFF0A1A4A);
  static const Color accentBlue      = Color(0xFF0D47A1);
  static const Color glowBlue        = Color(0xFF1565C0);

  // ── Home (morado/violeta) ───────────────────────────────────────────
  static const Color homeBackground  = Color(0xFF1A1035);
  static const Color appBarPurple    = Color(0xFF2D1B6B);
  static const Color cardDark        = Color(0xFF252040);
  static const Color navBarBg        = Color(0xFF1E1550);
  static const Color navBarActive    = Color(0xFF2A1F7A);
  static const Color drawerBg        = Color(0xFF1A1035);
  static const Color inputHomeBg     = Color(0xFF2D2050);
  static const Color inputHomeBorder = Color(0xFF4A3A8A);

  // ── Rojo principal ──────────────────────────────────────────────────
  static const Color primary         = Color(0xFFE53935);
  static const Color primaryDark     = Color(0xFFC62828);

  // ── Estados ─────────────────────────────────────────────────────────
  static const Color success         = Color(0xFF2E7D32);
  static const Color successLight    = Color(0xFF4CAF50);
  static const Color warning         = Color(0xFFF9A825);

  // ── Texto ───────────────────────────────────────────────────────────
  static const Color textPrimary     = Color(0xFF1A1A1A);
  static const Color textSecondary   = Color(0xFF757575);
  static const Color textHint        = Color(0xFFBDBDBD);
  static const Color textOnDark      = Colors.white;
  static const Color textOnPrimary   = Colors.white;

  // ── Card / Surface ──────────────────────────────────────────────────
  static const Color cardBg          = Colors.white;
  static const Color tabInactive     = Color(0xFFE0E0E0);
  static const Color inputBorder     = Color(0xFFE0E0E0);
  static const Color error           = Color(0xFFB00020);
  static const Color link            = Color(0xFF1565C0);
}
